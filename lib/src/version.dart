// GENERATED FILE — do not edit by hand.
//
// Regenerate with `dart run tool/sync_version.dart` after changing `version:`
// in pubspec.yaml. `test/version_test.dart` fails the build if the two drift.

/// The published version of this package.
///
/// Sent to the native SDK as `wrapper_version` during `initialize`, which
/// stamps it onto `setup.wrapper_version` in every event. Carrying it across
/// the channel from Dart keeps one source of truth: the alternative is
/// duplicating the version into `build.gradle.kts` and the podspec, where it
/// silently drifts because nothing fails when it does.
const String packageVersion = '0.2.1';

/// The wrapper type reported alongside [packageVersion].
const String packageWrapperType = 'flutter';
